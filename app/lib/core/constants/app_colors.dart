import 'package:flutter/material.dart';

class AppColors {
  // Palette mới theo Stitch + badge system
  static const Color primary      = Color(0xFF1A6FFF);
  static const Color secondary    = Color(0xFF5856D6); // Keep for compatibility
  static const Color accent       = Color(0xFFFF8C00); // cam thành tích
  static const Color sos          = Color(0xFFE53935); // đỏ SOS
  static const Color background   = Color(0xFFF5F5F5);
  static const Color cardWhite    = Color(0xFFFFFFFF);
  static const Color textPrimary  = Color(0xFF1C1C1E);
  static const Color textGrey     = Color(0xFF9E9E9E);
  static const Color sosLight     = Color(0xFFFFEBEE); // nền nút đăng xuất
  static const Color warning      = Color(0xFFFF9500);
  static const Color error        = Color(0xFFFF3B30);
  static const Color success      = Color(0xFF34C759);

  // Badge border colors (viền avatar theo huy hiệu — iSharing style)
  static const Color badgeNovice  = Color(0xFF9E9E9E); // Tân binh
  static const Color badgeBronze  = Color(0xFFFF8C00); // Hạng Đồng
  static const Color badgeSilver  = Color(0xFFB0BEC5); // Hạng Bạc
  static const Color badgeGold    = Color(0xFFFFD700); // Hạng Vàng
  static const Color badgeLegend  = Color(0xFF9C27B0); // Huyền thoại
}
